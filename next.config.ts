import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['res.cloudinary.com', 'plazavea.vteximg.com.br'], // Agrega aquí los dominios de tus imágenes
  },
};

export default nextConfig;
